Run the feature-lifecycle skill to orchestrate the full AI-DLC pipeline
(spec → design → implement → test → review → docs → release) with a mandatory
human checkpoint between every stage.

Input: $ARGUMENTS
